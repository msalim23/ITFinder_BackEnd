spring:
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    password: root
    url: jdbc:mysql://localhost:3306/itfinder
    username: root
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
